{% extends 'base.html.twig' %}

{% block body %}
    <h1>Encryptedvote</h1>

    <table>
        <tbody>
            <tr>
                <th>Encryptedvote</th>
                <td>{{ encryptedvote.encryptedVote }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ encryptedvote.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('encryptedvote_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('encryptedvote_edit', { 'id': encryptedvote.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
